# Only needed for `make local-up` (external domain overlay).
# `make up` works without .env.
DOMAIN=tostada.example.com
LISTEN_PORT=12025
GATEWAY_HOST=tostada-control-plane
GATEWAY_NODEPORT=30080
GUACAMOLE_JSON_SECRET_KEY=change-me-hex-key
